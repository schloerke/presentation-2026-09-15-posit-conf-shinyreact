"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
auto_run_after_setup = true
run_mode = "concurrent"

[scripts.run.slides]
available_in = [ "local" ]
command = "quarto preview index.qmd --port $CONDUCTOR_PORT --no-browser"
default = true
icon = "presentation"
