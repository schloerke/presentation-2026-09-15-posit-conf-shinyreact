---
title: "Beyond Bootstrap"
subtitle: "Building Custom Shiny UI with React"
author:
  - "Barret Schloerke"
  - "posit / Shiny Team"
  - "@schloerke"
# Nothing here is executed: the `shinylive-r` blocks are handled by a filter and
# mermaid by quarto itself. Say so, or quarto sees `{shinylive-r}`, guesses
# jupyter, and a machine without nbformat fails the render.
engine: markdown
format:
  revealjs:
    theme: [dark, theme/shinyreact-dark.scss]
    # No math in the deck, and the default MathJax comes off a CDN - which the
    # venue may not have. `plain` keeps the render fully self-contained.
    html-math-method: plain
    highlight-style: theme/shinyreact-dark.highlight.theme
    # 1920x1080 so every px in DESIGN.md and shinyreact-dark.scss is literal.
    width: 1920
    height: 1080
    margin: 0
    min-scale: 0.2
    max-scale: 2
    center: false
    slide-number: c
    footer: "posit::conf(2026) · @schloerke"
    code-line-numbers: false
    code-copy: false
    incremental: true
    # drop:                      # quarto-drop; off - it loads webR from a CDN.
    #   engine: webr             # See CLAUDE.md.
    #   button: true
    include-after-body:
      - theme/fit-width.html
      - theme/jsx-tokens.html
      - theme/gif-restart.html
      - theme/qr-links.html
    # Pre-render mermaid to svg: left to the browser, mermaid measures its
    # labels while reveal has the slide display:none'd and draws empty boxes.
    mermaid-format: svg
revealjs-plugins:
  # - drop       # quarto-drop (webR console on backtick). See CLAUDE.md.
  # - editable   # quarto-revealjs-editable; needs both lists. See CLAUDE.md.
filters:
  - shinylive
  # - editable
---

## [Old Faithful]{.fragment} {.divider data-number="DEMO"}

<!-- Notes before the first `##` become their own blank slide, so the opening
     narration lives on this divider rather than the title slide. -->
::: {.notes}
Today I want to talk about using React.js within your Shiny applications. But
before I do that, I want to talk about my favorite Shiny application. *Everyone
remembers their first… app*, and mine was Old Faithful.

I have been building Shiny apps since 2016 and been on the Shiny Team since
2018. The Old Faithful app is the kind of app that made me love Shiny: one
input, one plot, a complete interactive application in just a few lines of R.

And this app is still Old Faithful in another sense: dependable, familiar, and
exactly the UI we have all seen before.
:::

## Old Faithful, live {.demo-slide}

```{shinylive-r}
#| standalone: true
#| components: [viewer]
#| viewerHeight: 1080
{{< include apps/00-old-faithful-trim/app.R >}}
```
::: {.notes}
Let's dig into the code.
:::

## Ownership {.code-slide}

::: {.incremental}
- R owns both the UI definition **and** the reactive computation.
:::

::: {.columns .spaced}
::: {.column .fragment width="50%"}
```{.r filename="UI"}
ui <- bslib::page_sidebar(
  sidebar = bslib::sidebar(
    sliderInput(
      inputId = "bin_count",
      label = "Number of bins:",
      min = 1, max = 50,
      value = 30
    )
  ),
  plotOutput(outputId = "distPlot")
)
```
:::
::: {.column .fragment width="50%"}
```{.r filename="Server"}
server <- function(input, output) {
  output$distPlot <- renderPlot({
    x <- faithful$waiting
    hist(x, breaks = input$bin_count)
  })
}
```
:::
:::


::: {.notes}
The R UI is wonderful… until it isn't

- For an app like this, `bslib` gets you from nothing to something to be *proud of*
:::

## Ownership, moves to server {.code-slide .code-sm}

::: {.columns}
::: {.column .fragment width="38%"}
```{.r filename="UI"}
ui <- bslib::page_fillable(
  uiOutput("app")
)
```
:::
::: {.column .fragment width="62%"}
```{.r filename="Server"}
server <- function(input, output) {
  output$app <- renderUI({
    req(has_state())
    bslib::card(
      sliderInput("bin_count", "Bins:", 1, 50, 30),
      plotOutput("distPlot")
    )
  })

  output$distPlot <- renderPlot({
    x <- faithful$waiting
    hist(x, breaks = input$bin_count)
  })
}
```
:::
:::

::: {.x-mark .fragment}
```{=html}
<svg viewBox="0 0 1019 329" aria-hidden="true">
  <path pathLength="100" d="M 16 14 C 300 118, 690 208, 1003 315" />
  <path pathLength="100" d="M 1001 20 C 700 118, 320 214, 18 311" />
</svg>
```
:::

::: {.circle-mark .fragment}
```{=html}
<svg viewBox="0 0 940 370" aria-hidden="true">
  <path pathLength="100" d="M 903 253 C 892 342, 683 383, 463 378 C 232 373, 37 342,
    33 216 C 29 87, 235 24, 470 19 C 698 13, 899 68, 911 185 C 918 278, 852 339,
    760 370" />
</svg>
```
:::

::: {.notes}
- Then a request comes in:
  - a distinctive interface
  - richer interaction
  - a component from a modern design system
- Suddenly you're authoring an uphill battle of markup, styling, and interaction details

But the reactive data model is your domain expertise

- I do **not** want to replace Shiny

Keep Shiny for **reactive computation**.

Use a modern UI ecosystem — **React.js** — for the interface.
:::


## React.js {.divider data-number="02"}

## What is React? {.code-slide}

<!-- The atom marks every slide in section 02 as a React slide. `.hexreact` is
     atom-only by design (see the theme) and `.corner` parks it where
     `.hexlogo.corner` parks the finished mark. Never with a `data-id`: that is
     what enlists an element in the logo build's auto-animate chain. -->
<div class="hexreact corner"><div class="hr-atom"></div></div>

- > The library for web and native user interfaces
- > "…build user interfaces out of individual pieces called **components**"

<!-- One component, defined once and used twice - the smallest honest picture of
     "user interfaces out of individual pieces". The right-hand panel is not a
     screenshot: it is the deck's own `.stats` markup, the same class names the
     JSX writes, so what you read on the left is literally what draws on the
     right. 272 is `nrow(faithful)` and 30 is the app's default bin count. -->
::: {.columns}
::: {.column .fragment width="64%"}
```{.javascriptreact filename="Stat.jsx"}
function Stat({ label, value }) {
  return (
    <div className="stat">
      <span className="stat-label">{label}</span>
      <span className="stat-value">{value}</span>
    </div>
  );
}
<Stat label="Eruptions" value={272} />
<Stat label="Bins" value={30} />
```
:::
::: {.column .fragment width="36%"}
[Rendered]{.render-label}

::: {.render-out}
::: {.stats}
::: {.stat}
[Eruptions]{.stat-label}
[272]{.stat-value}
:::
::: {.stat}
[Bins]{.stat-label}
[30]{.stat-value}
:::
:::
:::
:::
:::

::: {.notes}
React calls itself "the library for web and native user interfaces", and you
build those interfaces out of individual pieces called components.

A component is a function that returns markup. Define `Stat` once, and you use
it as `<Stat>` — twice here, and it draws twice. That is the whole idea: you
name a piece of UI, and then you reuse it.
:::

## Why React? {.nostretch}

<div class="hexreact corner"><div class="hr-atom"></div></div>

<!-- The two sub-bullets are the evidence for "massive", so they are screenshot
     and number rather than more prose. The badge is github.com/facebook/react's
     own "Used by", screenshotted at 4x on 2026-09-06 (the raw count that day was
     30,088,518; the avatar row and that number are cropped off). Shot over an
     $ink ground, so only `.used-by`'s border marks it as a screenshot.
     The download numbers, also measured 2026-09-06: npm's `react` did
     171,637,376 in the week to 2026-08-29 (api.npmjs.org/downloads/point/
     last-week/react) against shiny's 50,465,284 since cranlogs began on
     2012-10-01 (cranlogs.r-pkg.org). Re-measure before the talk; the ratio
     only grows. -->
- **Massive** adoption in the JS community
  - GitHub: ![](images/react-used-by.png){.used-by fig-alt="GitHub's sidebar badge: Used by 30M"}
  - `react` alone pulled **170M downloads last week**\
    3x Shiny's 14-year CRAN total of 50M
- Proven component libraries, design systems, charts, tables, maps, ...

::: {.notes}
A MASSIVE ecosystem of proven components, ready for production environments. It
dwarfs what R and Python readily have available.

Thirty million repositories on GitHub depend on React.

And react alone pulled 170 million downloads *last week* — more than three times
Shiny's entire 14-year total on CRAN, which is about 50 million.

*Show off existing libraries.*
:::

## UI is assembled from data's state

<div class="hexreact corner"><div class="hr-atom"></div></div>

<!-- Old Faithful, twice: the raw waiting times plus a bin count, the `data`
     they reduce to, and the bars that data draws. Every number is real -
     `waiting` and the counts both come from apps/02-react-only/www/data.js -
     and the bars are written straight into the svg; no chart library for two
     static pictures. Both fragments are unindexed so they queue after the
     bullets in DOM order; see CLAUDE.md on why indices would hoist them. -->
::: {.r-stack .state-stack}
::: {.fragment .fade-in-then-out .state-viz}
[[`waiting = [79, 54, 74, 62, …]`]{.state-src}[`bin_count = 12`]{.state-count}]{.state-json} [→]{.state-arrow} [`bins = [13, 19, 31, 20, …]`]{.state-json} [→]{.state-arrow}
<svg viewBox="0 0 900 300" preserveAspectRatio="xMinYMid meet" role="img" aria-label="Histogram with 12 bars"><rect x="0" y="222" width="65.8" height="78" rx="3"/><rect x="75.8" y="186" width="65.8" height="114" rx="3"/><rect x="151.7" y="114" width="65.8" height="186" rx="3"/><rect x="227.5" y="180" width="65.8" height="120" rx="3"/><rect x="303.3" y="216" width="65.8" height="84" rx="3"/><rect x="379.2" y="264" width="65.8" height="36" rx="3"/><rect x="455" y="198" width="65.8" height="102" rx="3"/><rect x="530.8" y="0" width="65.8" height="300" rx="3"/><rect x="606.7" y="42" width="65.8" height="258" rx="3"/><rect x="682.5" y="72" width="65.8" height="228" rx="3"/><rect x="758.3" y="204" width="65.8" height="96" rx="3"/><rect x="834.2" y="270" width="65.8" height="30" rx="3"/></svg>
:::
::: {.fragment .state-viz}
[[`waiting = [79, 54, 74, 62, …]`]{.state-src}[`bin_count = 30`]{.state-count}]{.state-json} [→]{.state-arrow} [`bins = [1, 8, 7, 10, …]`]{.state-json} [→]{.state-arrow}
<svg viewBox="0 0 900 300" preserveAspectRatio="xMinYMid meet" role="img" aria-label="Histogram with 30 bars"><rect x="0" y="289" width="25.2" height="11" rx="3"/><rect x="30.2" y="211" width="25.2" height="89" rx="3"/><rect x="60.3" y="222" width="25.2" height="78" rx="3"/><rect x="90.5" y="189" width="25.2" height="111" rx="3"/><rect x="120.7" y="233" width="25.2" height="67" rx="3"/><rect x="150.8" y="167" width="25.2" height="133" rx="3"/><rect x="181" y="133" width="25.2" height="167" rx="3"/><rect x="211.2" y="222" width="25.2" height="78" rx="3"/><rect x="241.3" y="256" width="25.2" height="44" rx="3"/><rect x="271.5" y="156" width="25.2" height="144" rx="3"/><rect x="301.7" y="256" width="25.2" height="44" rx="3"/><rect x="331.8" y="222" width="25.2" height="78" rx="3"/><rect x="362" y="267" width="25.2" height="33" rx="3"/><rect x="392.2" y="267" width="25.2" height="33" rx="3"/><rect x="422.3" y="267" width="25.2" height="33" rx="3"/><rect x="452.5" y="200" width="25.2" height="100" rx="3"/><rect x="482.7" y="211" width="25.2" height="89" rx="3"/><rect x="512.8" y="233" width="25.2" height="67" rx="3"/><rect x="543" y="111" width="25.2" height="189" rx="3"/><rect x="573.2" y="0" width="25.2" height="300" rx="3"/><rect x="603.3" y="100" width="25.2" height="200" rx="3"/><rect x="633.5" y="156" width="25.2" height="144" rx="3"/><rect x="663.7" y="11" width="25.2" height="289" rx="3"/><rect x="693.8" y="122" width="25.2" height="178" rx="3"/><rect x="724" y="211" width="25.2" height="89" rx="3"/><rect x="754.2" y="233" width="25.2" height="67" rx="3"/><rect x="784.3" y="200" width="25.2" height="100" rx="3"/><rect x="814.5" y="278" width="25.2" height="22" rx="3"/><rect x="844.7" y="267" width="25.2" height="33" rx="3"/><rect x="874.8" y="289" width="25.2" height="11" rx="3"/></svg>
:::
:::

::: {.notes}
React is *REALLY* good at just taking JSON and mapping data to UI.

You describe what the UI looks like *for a given state* — and don't worry about
managing the transitions from one data state to another!

Old Faithful is the whole idea in one picture: the same 272 waiting times go in,
`bin_count` reduces them to a `bins` array, and that array *is* the chart. The
only thing that changed between these two is the number. Nobody added, removed,
or resized a bar.
:::

## Old Faithful {.demo-slide .demo-titled}

<!-- A static page, not shinylive: no server, no webR. `.demo-slide > iframe`
     in the theme sizes it to the full 1920x1080. -->
<iframe src="apps/02-react-only/www/index.html" title="Old Faithful, React only"></iframe>
<!-- After the iframe, not before: neither carries a z-index, so DOM order is
     what puts the atom over the app rather than under it. -->
<div class="hexreact corner"><div class="hr-atom"></div></div>

::: {.notes}
Same app, no Shiny at all — this is 100% React, and the data is baked into the
page.

Drag the slider. Every bar moves. I did not write a single line that adds,
removes, or resizes a bar.
:::

<!-- The atom in the corner is what says "React" now that the heading doesn't.
     `.hexreact` is atom-only by design (see the theme), and `.corner` puts it
     where `.hexlogo.corner` parks the finished mark. No `data-id`: this slide is
     not part of the logo build's auto-animate chain, and adding one would enlist
     it. The id is explicit because "Old Faithful" alone collides with the
     divider slide's generated `#old-faithful`. -->
## Old Faithful {#react-code .code-slide}

<div class="hexreact corner"><div class="hr-atom"></div></div>

```{.javascriptreact filename="app.tsx" code-line-numbers="|2|3,6|3,4,6|3,4,5,6|11-12|13"}
function App() {
  const [binCount, setBinCount] = useState(30);
  const bins = useMemo(
    () => bin_data(waiting, binCount),
    [binCount]
  );

  return (
    <main className="layout">
      <label htmlFor="bin_count">Number of bins:</label>
      <input id="bin_count" type="range" value={binCount}
             onChange={(e) => setBinCount(Number(e.target.value))} />
      <Histogram bins={bins} />
    </main>
  );
}
```

::: {.notes}
`bin_count` is the only state in the app. `bins` is *derived* from it — React
recomputes it when, and only when, `bin_count` changes.

Then the whole UI is described once, for whatever `bin_count` happens to be. New
state, new description, and React works out the difference.

Hold on to those two lines. In a few minutes the only thing that changes is
*where they get their values from*.
:::

## Why Shiny **+** React? {auto-animate="true" auto-animate-unmatched="false"}

<!-- The two marks as a corner lockup. They are the same `[data-id]` elements on
     the next two slides, so auto-animate carries them into the build and then
     into the finished logo. `.lb-shiny` is Shiny's own hex sticker, laid over
     the shinyreact logo; `.lb-hole` covers that logo's React atom, so what is
     underneath is the *Shiny* mark either way. See CLAUDE.md. -->
<div class="hexlogo lockup" data-id="hexlogo"><div class="lb-hole"></div><div class="lb-shiny"></div></div>
<div class="hexplus">+</div>
<div class="hexreact lockup" data-id="hexreact"><div class="hr-atom"></div></div>

- If you've found yourself writing custom HTML and JavaScript, React is the framework that scales those projects
- "We picked it up off the shelf to not write JS… happy with the tradeoffs"
- AI does better building on top of existing frameworks

::: {.notes}
If you've found yourself writing custom HTML and JavaScript, React.js is a
perfect framework to help scale your larger projects.
:::

## The mark {.logo-slide auto-animate="true" auto-animate-unmatched="false"}

<!-- The lockup, at full size. The Shiny sticker arrives with the move and then
     dissolves into the deck's own outlined mark; `.hexreact` is a
     `.fragment.fade-out`, so it is there on arrival and flies on the click.
     Both are in the theme, keyed off `.present` and `.visible`. -->
<div class="hexlogo" data-id="hexlogo"><div class="lb-hole"></div><div class="lb-shiny"></div></div>
<div class="hexreact fragment fade-out" data-id="hexreact"><div class="hr-atom"></div></div>

::: {.notes}
Two logos. One click, and they meet: the Shiny mark takes the middle, and React
comes round into the tail of the swoosh.

That *is* the logo. Nothing about Shiny changed — React is just what the swoosh
now ends in.
:::

## `shinyreact` {auto-animate="true" auto-animate-unmatched="false"}

<div class="hexlogo corner" data-id="hexlogo"></div>

*Shiny UI infrastructure for React-based component rendering*

- The Shiny server contains **only reactive data computation**
- The UI is a React client **you own**
- `shinyreact` is the bridge between the two — it ships **zero UI components**

::: {.notes}
With `shinyreact`, the Shiny server contains only reactive data computation, and
the UI is a React client you own. `shinyreact` provides the bridge between the
two and ships zero UI components itself.
:::

## Two frameworks, one reactive model

- Pure functional programming keeps logic easy to reason about as apps grow
- That's what makes *both* frameworks a joy to work in
- Build the whole UI from the ground up in React / TypeScript
- *Experimental package, for the brave few*

::: {.notes}
Pure functional programming makes logic easy to reason about as apps scale up in
complexity. This makes both of these frameworks a joy to work in.

Experimental package for the brave few.
:::

## "You want me to write JavaScript?!?"

- Joe: "Shiny authors shouldn't need to write JavaScript"
- Barret: **I believe this still holds true!**
- Agents have far more examples of mainstream React patterns than custom Shiny UI apps
  - I trust any frontier model to write React code better than I can
- Your job: **describe your UI, review the result**

::: {.notes}
At this point you might be thinking… "You want me to write JavaScript?!?"

Agents today have far more examples of mainstream React patterns than custom
Shiny UI apps. I trust any frontier model to write React.js code better than I
can. That narrow IDs-and-JSON contract makes the client boundary practical to
review and maintain.

You need to be able to describe your UI and review the result.
:::

## Old Faithful, remixed {.divider data-number="04"}

## The server we started with {.code-slide auto-animate="true"}

<!-- The trailing `|` is an extra step that clears the highlight, so the
     auto-animate into the next slide only has to move the changed lines. -->
```{.r filename="app.R" code-line-numbers="|5|6|"}
x <- faithful$waiting

server <- function(input, output) {
  output$distPlot <- renderPlot({
    breaks <- seq(min(x), max(x), length.out = input$bin_count + 1)
    hist(x, breaks = breaks)
  })
}
```

::: {.chain}
[`input$bin_count` → `breaks`]{.chain-1} [→ `hist()`]{.chain-2} [→ `renderPlot()`]{.chain-3}
:::

::: {.notes}
Let's re-imagine our Old Faithful app using `shinyreact`.

We can see the single input value `bin_count` being used to calculate `breaks`.
That value is then fed into `hist()` with the specified breaks to produce a
histogram of the data.
:::

## The same server, with `shinyreact` {.code-slide auto-animate="true"}

<!-- Opens on line 5 rather than unhighlighted: the chain arrives reading
     `input$bin_count → breaks`, so the line that computes it is lit to match. -->
```{.r filename="app.R" code-line-numbers="5|6|7|4,8|"}
x <- faithful$waiting

server <- function(input, output) {
  output$distPlot <- shinyreact::reactive_output({
    breaks <- seq(min(x), max(x), length.out = input$bin_count + 1)
    bins <- hist(x, breaks = breaks, plot = FALSE)
    bins[c("breaks", "counts")]
  })
}
```

<!-- The chain carries over from the previous slide, so its first two links are
     unwrapped (no `.chain-N` span = visible on arrival). `bins` lands on step 1
     (line 6) and `reactive_output()` on step 3 (lines 4, 8); step 2 adds no
     link. See CLAUDE.md on counting the steps. -->
::: {.chain}
`input$bin_count` → `breaks` [→ `bins`]{.chain-1} [→ `reactive_output()`]{.chain-3}
:::

<!-- .after-code, not .fragment: this appears on the same click as the code
     block's trailing `|` step, which clears the highlight. See the theme. -->
::: {.after-code}
The computation is unchanged — **the value sent changed.**
:::

::: {.notes}
The reactive computation is unchanged. What has changed is the value sent by the
server.
:::

## `reactive_output()` — a [new]{.underline} concept

- No corresponding UI component on the server side
- Convert your data and send it to the client **as JSON**
- `hist(x, plot = FALSE)` returns `breaks` and `counts` — send exactly what's needed
- That shape is **the contract** the browser receives
- Let React display your data's state in the UI

::: {.notes}
`hist(x, plot = FALSE)` creates a list containing `breaks` and `counts`. Now we
just send the required data — that is the contracted shape the browser receives.
Let React display your data's state within the UI.

There is no directly corresponding UI component. This is a new concept!

With this model, we are strongly encouraging the server to pass data directly to
the front end. Then let React handle the data from there.
:::

## The UI moves to TypeScript {.code-slide}

::: {.columns}
::: {.column width="50%"}
```{.r filename="app.R (classic)"}
ui <- bslib::page_sidebar(
  sidebar = bslib::sidebar(
    sliderInput(inputId = "bin_count", ...)
  ),
  plotOutput(outputId = "distPlot")
)
```
:::
::: {.column width="50%"}
```{.r filename="app.R (shinyreact)"}
ui <- shinyreact::page_react_html()
```

::: {.fragment}
UI inputs and outputs now live in TypeScript.
:::
:::
:::

::: {.notes}
Now let's look at the UI definitions.

Note that you're signing up for all UI inputs and outputs living in TypeScript!
:::

## `ui.tsx` {.code-slide}

::: {.incremental}
- Built upon `index.html`, but upgraded for React
:::

::: {.fragment}
```{.javascriptreact filename="www/ui.tsx" code-line-numbers="|2|3|8-9|10"}
export default function App() {
  const [binCount, setBinCount] = useShinyInput<number>("bin_count", 30);
  const bins = useShinyOutputValue<HistData | null>("distPlot", null);

  return (
    <main className="layout">
      <label htmlFor="bin_count">Number of bins:</label>
      <input id="bin_count" type="range" value={binCount}
             onChange={(e) => setBinCount(Number(e.target.value))} />
      <Histogram bins={bins} />
    </main>
  );
}
```
:::

::: {.notes}
`ui.tsx` is Shiny's `index.html` escape hatch, but upgraded for React.
:::

## Two hooks are the whole API {.code-slide}

::: {.fragment .spaced}
```{.javascriptreact filename="read and write an input"}
const [binCount, setBinCount] = useShinyInput<number>("bin_count", 30);
```
:::

::: {.fragment}
Just like `useState` — except the state comes from R / python.
:::

::: {.fragment .spaced}
```{.javascriptreact filename="read an output"}
const bins = useShinyOutputValue<HistData>("distPlot", null);
```
:::

::: {.fragment}
R computes the value; React decides how it looks.
:::

## Live: Old Faithful w/ `shinyreact` {.demo-slide .demo-titled}

<!-- The finished mark, in the corner it lands in on the section-03 bullets
     slide. No `data-id`: this slide is nowhere near the logo build's
     auto-animate chain, and adding one would enlist it. -->
<div class="hexlogo corner"></div>

```{shinylive-r}
#| standalone: true
#| components: [viewer]
#| viewerHeight: 1080

## file: app.R
# `shinyreact` (and `brio`) are mounted from the deck's own assets by
# bundle-wasm.R, so nothing is installed at runtime. See CLAUDE.md.
{{< include apps/01-shinyreact/app.R >}}

## file: www/index.html
## type: text
{{< include apps/01-shinyreact/www/index.html >}}

## file: www/app.css
## type: text
{{< include apps/01-shinyreact/www/app.css >}}

## file: www/app.js
## type: text
{{< include apps/01-shinyreact/www/app.js >}}
```

## The data cycle {.cycle-slide .nostretch}

```{mermaid}
%%{init: {"theme":"base","fontFamily":"Helvetica, Arial, sans-serif",
  "sequence":{"actorMargin":100,"width":300,"boxMargin":10,"height":50,
  "messageMargin":30,"mirrorActors":false},
  "themeVariables":{
  "actorBkg":"#141519","actorBorder":"#6FD4E8","actorTextColor":"#F2F4F8",
  "actorLineColor":"#A3ACBB","signalColor":"#00D8FF","signalTextColor":"#F2F4F8",
  "noteBkgColor":"#141519","noteBorderColor":"#A3ACBB","noteTextColor":"#F2F4F8",
  "labelBoxBkgColor":"#141519","labelTextColor":"#F2F4F8",
  "activationBkgColor":"#23252C","activationBorderColor":"#6FD4E8"}}}%%
sequenceDiagram
  participant B as browser · React
  participant S as server · R / Python
  B->>B: useShinyInput()
  B->>S: IDs + JSON
  S->>S: input$value → reactive() → reactive_output()
  S-->>B: IDs + JSON
  B->>B: useShinyOutputValue()
```

::: {.notes}
Top to bottom: a value leaves the browser through `useShinyInput()`, arrives as
`input$value`, moves through `reactive()` to `reactive_output()`, and comes back
to the browser as data through `useShinyOutputValue()`.

In Shiny for R or Python, that middle box is the app you already write:
`input$value` → `reactive()` → `renderFn()`. The only change is the last hop —
`reactive_output()` in place of `renderFn()`.

Keep R / Python for data work and reactivity. Inputs still travel to the Shiny
server; outputs now travel back as data.

IDs and JSON are the contract.
:::

## Testing {.divider data-number="05"}

## Test coverage at every hop

- **Within the server** — `test_server()`
  - set inputs, confirm output values
- **Server → client** — `wire_tap`
  - assert `shinyreact` messages on the websocket
- **Within the client** — JS unit tests
  - *agent's choice*
- **Confirm the client** — `features.md`
  - a nested list of features you can read and other agents can confirm

## Why that contract matters

- The narrow **IDs + JSON** boundary is practical to review and maintain
- Server logic stays in R: review it, defend it, test it independently
- Delegate the busy-work UI details to the agent — then verify

::: {.notes}
Agents today have far more examples of mainstream React patterns than custom
Shiny UI apps. I trust any frontier model to write React.js code better than I
can. That narrow IDs-and-JSON contract makes the client boundary practical to
review and maintain.

You need to be able to describe your UI and review the result.
:::

## In the wild {.divider data-number="06"}

::: {.notes}
None of this is theory any more. Over the summer one intern built a shelf of it.
:::

## Samuel Bharti[.com]{.dotcom} {#samuel-bharti .nostretch}

::: {.columns}
::: {.column width="32%"}
![](images/samuel-bharti.png){width="440"}
:::
::: {.column width="68%"}
::: {.nonincremental}
- Shiny summer '26 intern
- Ph.D. candidate, UAB
- Computational biologist
- 10x Shiny apps, and 4 packages under them
- [posit-dev/shiny-showcase-bioinformatics](https://github.com/posit-dev/shiny-showcase-bioinformatics){.gh}
:::

<!-- The QR sits under the gallery bullet, not under the headshot: it is the
     gallery's address, and next to the photo it would read as Samuel's. It
     encodes the *deployed* gallery, not the repo the bullet above links to —
     one bullet, two doors: the text is the source, the QR is the thing. It
     opens in a new tab, as the repo QR does: a click mid-talk must not
     navigate the deck away and lose the slide you were on. -->

[![](theme/qr-showcase.svg){.qr-inline}](https://posit-shiny-showcase-bioinformatics.share.connect.posit.cloud/){target="_blank" rel="noopener"}
:::
:::

::: {.notes}
Samuel was our software engineering intern this summer — a computational
biologist and a PhD candidate at UAB, previously at Genentech and Roche.

He spent the summer building bioinformatics Shiny apps, and the gallery is all
of them in one place — that is the QR, and the heading finishes his address on
its own if you wait a beat.
:::

<!-- Samuel's own framing, from his packages talk:
     https://github.com/samuelbharti/bio-packages/blob/main/slides/ — one line
     per package, in the order a request travels. `plotomics` is the fourth and
     is not part of that trio; it earns its line here because it is what
     Plotomics Live draws with two slides from now.

     The language badges are what each package actually ships, checked against
     the registries rather than against upstream's prose: biobouncer and
     plotomics are each on CRAN and PyPI; biohttp is CRAN-only; bioclients is
     R-only and on r-universe rather than CRAN. Both of the first two are also
     published to npm, but that is deliberately *not* tagged: the JS is what
     the R and Python packages are built out of, not a third thing an R user
     would reach for. -->
## Summer Packages {#bio-packages .pkg-slide .nostretch}

<!-- Same construction as `.app-slide`: one-item lists so each bullet is its own
     fragment, matching `fragment-index` on the hex so the two land on one
     click, `.nonincremental` so the `li` inside the div is not fragmented
     again, and `.nostretch` so quarto does not lift the images out of their
     `<p>`. Unlike the app slide the hexes accumulate, so they are plain
     `.fragment`s rather than `.fade-in-then-out`. -->
::: {.columns}
::: {.column width="62%"}
::: {.fragment .nonincremental fragment-index=1}
- [biobouncer](https://www.samuelbharti.com/biobouncer/r/) [![](images/lang-r.svg)![](images/lang-py.svg)]{.langs} – Is this a valid ID?
:::
::: {.fragment .nonincremental fragment-index=2}
- [bioclients](https://www.samuelbharti.com/bioclients/) [![](images/lang-r.svg)]{.langs} – What do the sources say?
:::
::: {.fragment .nonincremental fragment-index=3}
- [biohttp](https://www.samuelbharti.com/biohttp/) [![](images/lang-r.svg)]{.langs} – Move bytes, cache results
:::
::: {.fragment .nonincremental fragment-index=4}
- [plotomics](https://www.samuelbharti.com/plotomics/) [![](images/lang-r.svg)![](images/lang-py.svg)]{.langs} – 26 GPU-accelerated charts
:::
:::
::: {.column width="38%"}
<!-- One paragraph, four images: the `<p>` quarto wraps them in is what the
     grid is declared on, so the hexes are its items and `nth-child` counts
     them. All four logos are pointy-top hexes on a 173.2x200 viewBox (0.866),
     which is what lets the second row interlock at exactly -0.25 of a height.
     biobouncer's own logo.svg is padded inside a 440x500 box, so the vendored
     copy is cropped to the hex itself - otherwise it packs a size small. -->
::: {.hexpack}
![](images/pkg-biobouncer.svg){.fragment fragment-index=1}
![](images/pkg-bioclients.svg){.fragment fragment-index=2}
![](images/pkg-biohttp.svg){.fragment fragment-index=3}
![](images/pkg-plotomics.svg){.fragment fragment-index=4}
:::
:::
:::

::: {.notes}
Four packages came out from under those apps, and each one answers a single
question.

`biobouncer` — is this even an identifier? Fifty sources, offline first, and
one shared conformance corpus across R, Python and JavaScript, so the three
cannot disagree. It is on CRAN, PyPI and npm. `bioclients` —
what do the sources say about it? Twenty-nine biological web services, one
table shape. `biohttp` is what those clients ride on: every call returns a
value rather than raising, with retry, a circuit breaker, and a cache that
stores only the successes.

And `plotomics` — GPU-accelerated visualizations from one TypeScript core,
wrapped three ways: npm for React, htmlwidgets for R, anywidget for Python.
Headless factories, not React components, which is what lets one core serve
all three. Seventeen chart *types* in the package; twenty-six of them on
screen in the app you are about to see. Hold onto that one: it is what draws
the app two slides from now.
:::

## Summer Bioinformatics Apps {.app-slide .nostretch}

<!-- One screenshot per app, swapped on the same click as its bullet. Both
     halves carry matching `fragment-index`, which is the only way to land two
     fragments on one click; the images are `.fade-in-then-out` so exactly one
     is up at a time. Screenshots, not iframes: these are deployments, and the
     deck makes no off-origin request. The name links to the deployment and the
     sub-bullet to the source: the deck is published, so on a projector these
     read as plain text, and afterwards they are how a viewer gets to the app. -->
::: {.columns}
::: {.column width="46%"}
::: {.fragment .nonincremental fragment-index=1}
- **[tahoe-explorer](https://posit-tahoe-explorer.share.connect.posit.cloud/)** · 100.6M rows
  - Summaries push into DuckDB · [source](https://github.com/samuelbharti/tahoe-explorer){.gh}
:::
::: {.fragment .nonincremental fragment-index=2}
- **[genescout](https://posit-genescout.share.connect.posit.cloud/)** · evidence review
  - A ranked, cited shortlist · [source](https://github.com/samuelbharti/genescout){.gh}
:::
::: {.fragment .nonincremental fragment-index=3}
- **[variant-reviewer](https://posit-variant-reviewer.share.connect.posit.cloud/)** · 18 cards
  - Public APIs, fanned out async · [source](https://github.com/samuelbharti/variant-reviewer){.gh}
:::
::: {.fragment .nonincremental fragment-index=4}
- **[gene-list-builder](https://posit-gene-list-builder.share.connect.posit.cloud/)** · 7 sources
  - Re-ranks as you tune the weights · [source](https://github.com/samuelbharti/gene-list-builder){.gh}
:::
::: {.fragment .nonincremental fragment-index=5}
- **[Plotomics Live](https://posit-plotomics-live.share.connect.posit.cloud/)** · `shinyreact`
  - 1M WebGL points, one line of UI · [source](https://github.com/samuelbharti/plotomics-live){.gh}
:::
:::
::: {.column width="54%"}
::: {.r-stack .app-stack}
![](images/app-tahoe-explorer.png){.fragment .fade-in-then-out fragment-index=1}

![](images/app-genescout.png){.fragment .fade-in-then-out fragment-index=2}

![](images/app-variant-reviewer.png){.fragment .fade-in-then-out fragment-index=3}

![](images/app-gene-list-builder.png){.fragment .fade-in-then-out fragment-index=4}

![](images/app-plotomics-live.png){.fragment fragment-index=5}
:::
:::
:::

::: {.notes}
Nine apps in the gallery; these are the five with public source. Every name
here is a link — the app name goes to the deployment on Connect Cloud, the
sub-bullet to the source — and each one is archived on Zenodo too, so there is
a DOI to cite as well as an address. The README of this repo has all three
columns.

Four of these are ordinary Shiny: `shiny` plus `bslib`, the UI written in R.
That is the point — the last one is the only one that reaches for
`shinyreact`, and it does it because the visualization demanded it.
:::

## Plotomics Live {.gif-slide .nostretch}

<!-- The plotomics hex, parked in the corner the heading leaves free: this is
     the app the package from two slides ago draws. Markup rather than a
     `url()` in the scss, because quarto does not trace url()s out of an scss
     file and `images/` is not a project resource. -->
![](images/pkg-plotomics.svg){.corner-hex}

[**1,000,000** transcripts on the GPU · 26 visualizations · 65+ `reactive_output()`]{.gif-caption}

<!-- A recording of the deployment, not an iframe: it is a Connect Cloud app,
     and a served render makes no off-origin request. An `<video>` rather than
     a GIF - a million-point field is nearly incompressible as GIF (11 MB at 48
     colours and 6fps, and posterized), where h264 holds full colour at 25fps
     in 4 MB. Local file, so it is still offline-safe. See CLAUDE.md. -->
```{=html}
<video class="app-gif" src="images/plotomics-live.mp4"
       autoplay loop muted playsinline></video>
```

::: {.notes}
Xenium single-molecule transcripts. Every point is one mRNA molecule at the
micrometre where it was detected in a breast cancer section — not a cell, not
an embedding. The run found 42.6 million of them across a 313-gene panel, and
a million of those are drawn here, on the GPU, from React.

Watch the loop. They arrive — a million points streamed as binary blobs and
painted in one go. Hover one and you get the molecule under the cursor. Then
zoom, and back out to the whole section: the red is tumour epithelium, the
teal is stroma, and none of that structure is drawn by anything in R. R sent
the numbers.

The ggplot2 toggle is right there in the same panel, and on this page it has
to fall back to a subsample. That contrast is the argument.

Twenty-six visualizations, and the server publishes sixty-nine
`reactive_output()` feeds to serve them: twenty-two data feeds React reads
through `useShinyOutputValue`, twenty-eight ggplot2 PNGs for the classic side,
and the rest stats and config.

Two things to say out loud about that:

- `app.R` is **compute-only** — one `reactive_output()` per visualization, and
  not one line of it is UI
- the entire UI, in R, is **one line**: `page_react_html("www/index.html")`
:::

## What's next {.divider data-number="07"}

## Future work: incremental adoption

- Embed React components into **existing** apps
  - no need to port your whole application to React
- Opens the door for helper packages down the road
- Today, writing `shinyreact` apps will feel ad-hoc — that's the tradeoff of experimental

::: {.notes}
Incremental adoption makes it so you don't have to port your whole application
into React.js. It also opens the door for helper packages down the road.

Today, everything will feel ad-hoc when writing `shinyreact` apps.
:::

## Beyond Bootstrap {.recap}

- Keep **R / Python** for reactive data processing
- Let an **agent** build the React.js client
- **IDs and JSON structures** are the communication contract
- [posit-dev/shinyreact](https://github.com/posit-dev/shinyreact){.gh}

<!-- The lockup the title slide builds from `author:`, written out - this is not
     a quarto title slide, so there is nothing to reuse. -->
::: {.handle}
Barret Schloerke

posit / Shiny Team

@schloerke
:::

## Q & A {.divider data-number="08"}

## Is node.js needed?

- **No** — but highly recommended
- TSX is much more readable than vanilla JavaScript
- A build system can lint and strongly type your code

## How do I review generated code?

- Unit tests help with drift — but they confirm *existing* behavior
- The logic you care about lives server-side: review it, defend it, write independent tests
- I'm comfortable delegating busy-work UI details to the LLM

## What about my existing apps?

- Agent Skills are available to convert Shiny apps to `shinyreact`

## Can I keep my existing outputs?

- **Yup.** Use regular render methods, just like normal
- Pair them with `<ShinyOutput id="distPlot" />` in your React code
- Don't reinvent the wheel — your UI app becomes more about **layout** than outputs

## Thanks {.divider .end data-number="09"}

::: {.handle}
@schloerke
:::
